interface:
  display_name: "Jizhang Rebuild"
  short_description: "重建、迁移和恢复记账 PWA"
  brand_color: "#0F766E"
  default_prompt: "Use $jizhang-rebuild to verify, repair, redeploy, migrate, or import historical data for my Jizhang accounting app."
policy:
  allow_implicit_invocation: true
